{
    "query": "mutation import($schemaVersion:Float!, $tenant:String!, $name:String!, $namespace:String!, $version:Float!, $content:ContentPackTypeInput!) { importContentPack(schemaVersion:$schemaVersion, tenant:$tenant, name:$name, namespace:$namespace, version:$version, content:$content) {  name  } }",  "operationName": "import",
    "variables": {
        "schemaVersion": "{{ schemaVersion }}"
        "tenant": "{{ tenant }}",
        "name": "{{ name }}",
        "namespace": "{{ namespace }}",
        "version": "{{ version }}",
        "contentPack": {
            @@include('headers/default.njk')
            @@include('extracted_fields/default.njk')
            @@include('queries/default.njk')
            @@include('alerts/default.njk')
            @@include('dashboard_sections/default.njk')
            @@include('agent_classes/default.njk')
            @@include('alias_fields/default.njk')
            @@include('alias_rules/default.njk')
            @@include('footers/default.njk')
        }
    }
}
